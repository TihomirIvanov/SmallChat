<% publish_to "/messages/new" do %>
  var m = $( "#messages" );
  var scrollPosition = m.last().position().top + m.last().height();

  m.append( "<%= j render(@message) %>" );
  m.scrollTop( scrollPosition );

  $( "#new_message" )[0].reset();
<% end %>
